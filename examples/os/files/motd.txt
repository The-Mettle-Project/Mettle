welcome to mettle os.

this text is a file in the boot image. the build script packed it after the
kernel, the boot sector read it off the floppy, and ls and cat read it out of
memory. put your own files in examples/os/files and rebuild.
