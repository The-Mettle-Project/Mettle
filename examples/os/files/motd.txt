welcome to mettle os.

this text is a file on the disk. the build script packed it into an archive at
sector 1024, and the kernel read it back with its own ata driver rather than
asking the firmware. put your own files in examples/os/files and rebuild.

the shell runs in this window. try help, disk, mem, ps, ls, cat about.txt
