
-- optimization report: explain_demo.mettle ----------
  no optimization changes since the last explain build

  where to start (11 of 14 missed optimizations have a fix; "proven" = the compiler applied it to a clone and re-checked):
    1. proven matvec:38      hoist invariant index math into a pointer before the loop (e.g. `var row: float32* = ...
    2. proven sum_bytes:27   declare the accumulator as int64 (sum bytes as `total = total + (int64)data[i]`)
    3. proven sum_ints:76    declare the accumulator `s` as int64
    4. proven sum_shorts:86  use int32 elements
    5.        apply_bias:61  vectorizable shapes are unit-stride accesses (a[i], not a[i*k]) over ...
       ... and 6 more below

  saxpy (loop @ line 12): vectorized -> vfmadd231ps, 8-wide float32 affine map  [vectorized]
  with_call (loop @ line 19): vectorized -> vfmadd231ps, 8-wide float32 affine map  [vectorized]
  with_call (call to `scale` @ line 20): inlined  [inlined]
  sum_bytes (loop @ line 27): NOT vectorized  [byte-sum-narrow-acc]
      27 | for i in 0..n {
      28 |     total = total + (int32)data[i];
      \_ reason: this is a byte-sum loop, but the vpsadbw kernel accumulates into int64 and this loop's accumulator is narrower
      \_ fix: declare the accumulator as int64 (sum bytes as `total = total + (int64)data[i]`)
      \_ verified: simulated that fix and re-ran the optimizer: this loop then vectorizes -> vpsadbw, 32-wide byte sum (AVX2)
  matvec (loop @ line 36): NOT vectorized  [outer-of-nest]
      36 | for r in 0..rows {
      \_ reason: the body contains a nested loop (line 38), and only innermost loops are vectorized; the inner loop did not vectorize either -- see its remark
  matvec (loop @ line 38): NOT vectorized  [dot-shape-address]
      38 | for c in 0..cols {
      39 |     acc = acc + m[r * cols + c] * x[c];
      \_ reason: this is a float multiply-accumulate (dot-product shape), but no kernel matched its address pattern -- the bases must be plain pointers indexed by the loop counter (base[i])
      \_ fix: hoist invariant index math into a pointer before the loop (e.g. `var row: float32* = &m[r * cols];` then `row[c]`)
      \_ verified: simulated that fix and re-ran the optimizer: this loop then vectorizes -> vfmadd231ps, 8-wide float32 FMA dot product
  tiny_fixed (loop @ line 47): fully unrolled (8 iterations, constant trip count)  [unrolled]
  apply_bias (loop @ line 61): NOT vectorized  [unrecognized-shape]
      61 | for i in 0..n {
      62 |     s = s + a[i] + bias(k);
      \_ reason: no vectorizer recognized this loop's shape
      \_ fix: vectorizable shapes are unit-stride accesses (a[i], not a[i*k]) over int8/int32/float32/float64 with a straight-line body: maps (a[i] = expr), '+' reductions (s = s + expr), and dot products
  apply_bias (call to `bias` @ line 62): inlined  [inlined]
  sum_ints (loop @ line 76): NOT vectorized  [int32-sum-narrow-acc]
      76 | for i in 0..n {
      77 |     s = s + a[i];
      \_ reason: this loop sums int32 values into `s`, but the int32 reduction kernel accumulates into int64 (eight lanes are summed without overflow only there) and `s` is int32
      \_ fix: declare the accumulator `s` as int64
      \_ verified: simulated that fix and re-ran the optimizer: this loop then vectorizes -> vpaddd, 8-wide int32 sum (AVX2)
  sum_shorts (loop @ line 86): NOT vectorized  [int16-elements]
      86 | for i in 0..n {
      87 |     s = s + (int32)a[i];
      \_ reason: the loop reads/writes 16-bit integers, and no 16-bit kernels exist
      \_ fix: use int32 elements
      \_ verified: simulated that fix and re-ran the optimizer: this loop then vectorizes -> vpaddd, 8-wide int32 sum (AVX2)
  apply_damp (loop @ line 99): vectorized -> vfmadd231ps, 8-wide float32 affine map  [vectorized]
  apply_damp (call to `damp` @ line 100): inlined  [inlined]
  skew (loop @ line 109): NOT vectorized  [dot-shape-address]
     109 | for c in 0..n {
     110 |     acc = acc + m[k + c] * v[c];
     111 |     k = k + 2;
      \_ reason: this is a float multiply-accumulate (dot-product shape), but no kernel matched its address pattern -- the bases must be plain pointers indexed by the loop counter (base[i])
      \_ fix: none via hoisting -- re-checked: the index half that is not the loop counter changes every iteration, so it cannot be hoisted out; this access is genuinely non-unit-stride
  chain (call to `step1` @ line 123): inlined  [inlined]
  chain (call to `step1` @ line 123): inlined  [inlined]
  chain (call to `step1` @ line 123): inlined  [inlined]
  lcg_hash (loop @ line 133): NOT vectorized  [serial-recurrence]
     133 | while (i < n) {
     134 |     h = (h * 6364136223846793005 + 1442695040888963407) ^ (h >> 13);
     135 |     i = i + 1;
      \_ reason: `h` carries a loop-carried recurrence: each iteration computes it from its own previous value (through `*`, `+`, `>>`, `^`), so the iterations form a dependency chain that cannot run as independent SIMD lanes
      \_ fix: '+'/'-' reductions reassociate and DO vectorize; multiply, divide, shift, and bitwise/xor recurrences are inherently serial -- if this running state IS the algorithm (a hash, an RNG, an IIR filter), the loop is already at its scalar floor
  main (loop @ line 146): NOT vectorized  [store-only-fill]
     146 | while (i < 64) {
     147 |     xs[i] = (float32)i;
     148 |     ys[i] = 1.0;
     149 |     bs[i] = (uint8)i;
     150 |     i = i + 1;
      \_ reason: the loop only writes an invariant value (a fill/init pattern), but its store address did not match the fill vectorizer's shapes: a unit-stride element `a[i]`, `a[c + i]` with `c` a loop-invariant scalar, or a pointer walked by a constant stride
      \_ fix: hoist the invariant part of the index into a base pointer before the loop (`var row = &a[c]; ... row[i] = v;`) so the write is a plain unit-stride `row[i]`
  main (loop @ line 153): NOT vectorized  [store-only-fill]
     153 | while (i < 256) {
     154 |     mm[i] = 0.5;
     155 |     i = i + 1;
      \_ reason: the loop only writes an invariant value (a fill/init pattern), but its store address did not match the fill vectorizer's shapes: a unit-stride element `a[i]`, `a[c + i]` with `c` a loop-invariant scalar, or a pointer walked by a constant stride
      \_ fix: hoist the invariant part of the index into a base pointer before the loop (`var row = &a[c]; ... row[i] = v;`) so the write is a plain unit-stride `row[i]`
  main (call to `saxpy` @ line 157): inlined  [inlined]
  main (call to `with_call` @ line 158): NOT inlined  [callee-has-kernel]
     158 | with_call(&ys[0], 64);
      \_ reason: the callee's loops were vectorized into SIMD kernels after inlining ran; it stays a real call (the kernel runs the same either way)
  main (call to `matvec` @ line 159): inlined  [inlined]
  main (loop @ line 162): NOT vectorized  [store-only-fill]
     162 | while (i < 64) { iv[i] = i; i = i + 1; }
      \_ reason: the loop only writes an invariant value (a fill/init pattern), but its store address did not match the fill vectorizer's shapes: a unit-stride element `a[i]`, `a[c + i]` with `c` a loop-invariant scalar, or a pointer walked by a constant stride
      \_ fix: hoist the invariant part of the index into a base pointer before the loop (`var row = &a[c]; ... row[i] = v;`) so the write is a plain unit-stride `row[i]`
  main (call to `sum_bytes` @ line 163): inlined  [inlined]
  main (call to `apply_bias` @ line 163): inlined  [inlined]
  main (loop @ line 166): NOT vectorized  [int16-elements]
     166 | while (i < 64) { sh[i] = (int16)i; i = i + 1; }
      \_ reason: the loop reads/writes 16-bit integers, and no 16-bit kernels exist
      \_ fix: use int32 (or int8 if the values fit)
  main (call to `sum_ints` @ line 167): inlined  [inlined]
  main (call to `sum_shorts` @ line 167): inlined  [inlined]
  main (call to `apply_damp` @ line 168): inlined  [inlined]
  main (call to `norm2` @ line 169): inlined  [inlined]
  main (call to `chain` @ line 169): inlined  [inlined]
  main (call to `skew` @ line 169): inlined  [inlined]
  main (call to `opaque` @ line 170): inlined  [inlined]
  main (call to `tiny_fixed` @ line 170): inlined  [inlined]
  main (call to `lcg_hash` @ line 171): NOT inlined  [callee-has-loop]
     171 | u = u + (int32)lcg_hash((int64)u, 16);
      \_ reason: the callee contains a loop the inliner currently declines (a compiler limitation, not a problem in your code; the call itself costs little next to the loop inside it)
  the bracketed id after a verdict has a longer explanation: mettle explain byte-sum-narrow-acc


-- memory report: explain_demo.mettle ----------
  no memory diagnostics in this file


-- backend report: explain_demo.mettle ----------
  17/19 functions reaching codegen (after inlining) compiled with the register-allocating backend
  40.2% of the program's 480 optimized IR instructions are in register-allocated code

  2 functions use baseline (spill-everything) codegen, grouped by cause, largest first:

  contains the SIMD kernel `simd_affine_map_f32` in a form the register allocator's inline passthrough doesn't cover yet (2 functions, 287 instructions):
      \_ consequence: the kernel itself runs at full vector speed; only the scalar code around it keeps values on the stack
      \_ fix: nothing needed for a small function; a different shape of the same kernel (a simpler fill/map) inlines into register-allocated code
      \_ main (282), saxpy (5)

