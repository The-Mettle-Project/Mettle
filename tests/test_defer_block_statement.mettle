import "std/io";

fn before() { puts("BEFORE\n"); }
fn inside() { puts("INSIDE\n"); }
fn after() { puts("AFTER\n"); }

fn main() -> int32 {
  before();
  defer {
    inside();
    after();
  }
  return 0;
}
