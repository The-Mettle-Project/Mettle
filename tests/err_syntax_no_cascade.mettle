// One missing '(' after 'if'. Recovery stays inside the body, so this file
// reports one error and not a run of them from file scope.
fn main() -> int32 {
  var x: int64 = 1;
  if x > 1 {
    return 1;
  }
  return 0;
}
