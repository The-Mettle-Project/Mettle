import "std/io";

fn iter_defer() { puts("ITER_DEFER\n"); }
fn iter_body() { puts("ITER_BODY\n"); }

fn main() -> int32 {
  var i: int32 = 0;
  while (i < 2) {
    defer iter_defer();
    iter_body();
    i = i + 1;
  }
  return 0;
}
