struct Op { name: string; code: int32; }

var ops: Op[1] = [ { name: "add", code: 1 } ];

comptime for op in ops.rows {
  fn ident("code_of_", op.name)() -> int32 { return op.code; }
}

fn main() -> int32 {
  return code_of_add();
}
