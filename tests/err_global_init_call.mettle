// A global's initializer is laid out at compile time; there is no module
// initializer to run a call in. Must be a diagnostic with a source location,
// not an internal compiler error from codegen.

fn compute() -> int32 {
  return 7;
}

var limit: int32 = compute();

fn main() -> int32 {
  return limit;
}
