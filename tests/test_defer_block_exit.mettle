import "std/io";

fn inner_defer() { puts("INNER\n"); }
fn after_block() { puts("AFTER\n"); }
fn outer_defer() { puts("OUTER\n"); }

fn main() -> int32 {
  defer outer_defer();

  {
    defer inner_defer();
  }

  after_block();
  return 0;
}
